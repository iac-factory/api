{
    "name": "@iac-factory/api-middleware",
    "version": "0.0.1",
    "private": true,
    "description": "...",
    "author": "Jacob B. Sanders <jacob.sanders@cloudhybrid.io> (https://github.com/iac-factory)",
    "license": "BSD-3-Clause",
    "scripts": {
        "start": "cd $(git rev-parse --show-toplevel) && npm run start",
        "build": "tsc --build $(dirname $(npm root)/tsconfig.json"
    },
    "dependencies": {
        "body-parser": "^1.20.0",
        "compression": "^1.7.4",
        "esm": "^3.2.25"
    },
    "devDependencies": {
        "@types/compression": "^1.7.2",
        "@types/esm": "^3.2.0"
    },
    "eslintConfig": {
        "settings": {
            "extends": "../.."
        }
    }
}
